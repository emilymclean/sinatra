<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>
        <activity
            android:name=".upcoming.UpcomingVehiclesConfigurationActivity"
            android:exported="true" />

        <receiver
            android:name=".upcoming.UpcomingVehiclesWidgetReceiver"
            android:exported="true"
            android:label="@string/upcoming_vehicle_widget_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/upcoming_vehicles_widget_info" />
        </receiver>
    </application>

</manifest>